# Medic Card Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить карту Медик — юнит с механикой лечения ослабленных союзников.

**Architecture:** `healUnit` в движке (`GwentMatch.js`) лечит конкретную карту по индексу. Данные карты живут в `starterDecks.js`. `BattleScene` управляет состоянием `awaitingHeal` и рендерит зелёные цели для клика.

**Tech Stack:** Phaser 3, Vite, Vitest (тесты движка только)

## Global Constraints

- Файл движка: `src/engine/GwentMatch.js`
- Тесты движка: `src/engine/GwentMatch.test.js`
- Данные колоды: `src/data/starterDecks.js`
- UI сцена: `src/scenes/BattleScene.js`
- Запуск тестов: `npm test` (в папке `blood-wolf`)
- Phaser-сцены юнит-тестами не покрываем — проверяем вручную в браузере

---

### Task 1: healUnit — движок (TDD)

**Files:**
- Modify: `src/engine/GwentMatch.js`
- Modify: `src/engine/GwentMatch.test.js`

**Interfaces:**
- Produces: `export function healUnit(match, playerIndex, row, cardIndex): void`

- [ ] **Step 1: Написать падающие тесты**

Добавить в конец `src/engine/GwentMatch.test.js`:

```js
describe('healUnit', () => {
  it('restores a weakened unit to its base power', () => {
    const unit = { id: 'u', type: 'unit', row: 'melee', power: 5 };
    const filler = { id: 'f', type: 'unit', row: 'melee', power: 1 };
    const match = createMatch([unit], [filler], 1);
    playCard(match, 0, 'melee');
    match.players[0].board.melee[0].power = 2; // simulate sign_damage
    healUnit(match, 0, 'melee', 0);
    expect(match.players[0].board.melee[0].power).toBe(5);
  });

  it('throws when card index does not exist', () => {
    const unit = { id: 'u', type: 'unit', row: 'melee', power: 5 };
    const filler = { id: 'f', type: 'unit', row: 'melee', power: 1 };
    const match = createMatch([unit], [filler], 1);
    playCard(match, 0, 'melee');
    expect(() => healUnit(match, 0, 'melee', 9)).toThrow('No card at melee[9]');
  });

  it('throws when card is a hero', () => {
    const hero = { id: 'h', type: 'hero', row: 'melee', power: 7 };
    const filler = { id: 'f', type: 'unit', row: 'melee', power: 1 };
    const match = createMatch([hero], [filler], 1);
    playCard(match, 0, 'melee');
    match.players[0].board.melee[0].power = 4; // force-weaken to test the guard
    expect(() => healUnit(match, 0, 'melee', 0)).toThrow('Cannot heal a hero');
  });

  it('throws when card is not weakened', () => {
    const unit = { id: 'u', type: 'unit', row: 'melee', power: 5 };
    const filler = { id: 'f', type: 'unit', row: 'melee', power: 1 };
    const match = createMatch([unit], [filler], 1);
    playCard(match, 0, 'melee');
    expect(() => healUnit(match, 0, 'melee', 0)).toThrow('Card is not weakened');
  });
});
```

Добавить `healUnit` в импорт в верхней строке тестового файла:
```js
import { createMatch, playCard, pass, hasLegalMove, healUnit } from './GwentMatch.js';
```

- [ ] **Step 2: Убедиться что тесты падают**

```bash
cd C:/Users/user/source/repos/blood-wolf
npm test
```

Ожидаем: 4 новых теста FAIL с `healUnit is not a function`.

- [ ] **Step 3: Реализовать healUnit в GwentMatch.js**

Добавить в конец `src/engine/GwentMatch.js` (после функции `pass`):

```js
export function healUnit(match, playerIndex, row, cardIndex) {
  const card = match.players[playerIndex]?.board[row]?.[cardIndex];
  if (!card) throw new Error(`No card at ${row}[${cardIndex}]`);
  if (card.def.type === 'hero') throw new Error('Cannot heal a hero');
  if (card.power === card.def.power) throw new Error('Card is not weakened');
  card.power = card.def.power;
}
```

- [ ] **Step 4: Убедиться что все тесты проходят**

```bash
npm test
```

Ожидаем: все 89 тестов PASS (85 старых + 4 новых).

- [ ] **Step 5: Коммит**

```bash
cd C:/Users/user/source/repos/blood-wolf
git add src/engine/GwentMatch.js src/engine/GwentMatch.test.js
git commit -m "feat(engine): add healUnit — restore weakened unit to base power"
```

---

### Task 2: Данные карты Медик

**Files:**
- Modify: `src/data/starterDecks.js`

**Interfaces:**
- Consumes: `function card(id, name, type, row, power, effect, rarity, art)` — уже существует в файле
- Produces: карта `medic` в `PLAYER_DECK` с `effect: 'heal'`

- [ ] **Step 1: Добавить медика в PLAYER_DECK**

В `src/data/starterDecks.js`, добавить строку после `clear_sky`:

```js
  card('medic',    'Медик',   'unit',    'melee',  4, 'heal',     'rare',   'medic'),
```

Результат — конец `PLAYER_DECK`:
```js
  card('clear_sky', 'Ясное небо', 'special', 'melee', 0, 'clear', 'common', null),
  card('medic',    'Медик',   'unit',    'melee',  4, 'heal',   'rare',   'medic'),
];
```

- [ ] **Step 2: Убедиться что тесты проходят**

```bash
npm test
```

Ожидаем: все 89 тестов PASS (карта добавлена, ничего не сломано).

- [ ] **Step 3: Коммит**

```bash
git add src/data/starterDecks.js
git commit -m "feat(data): add Medic card to PLAYER_DECK"
```

---

### Task 3: UI — awaitingHeal в BattleScene

**Files:**
- Modify: `src/scenes/BattleScene.js`

**Interfaces:**
- Consumes: `healUnit(match, playerIndex, row, cardIndex)` из Task 1
- Consumes: `ROW_NAMES` из `../ui/layout.js` (уже импортирован)

- [ ] **Step 1: Добавить healUnit в импорт**

В `src/scenes/BattleScene.js`, строка 2 — изменить импорт:

```js
import { createMatch, playCard, pass, healUnit } from '../engine/GwentMatch.js';
```

- [ ] **Step 2: Инициализировать awaitingHeal в create()**

В методе `create(data)`, после строки `this.rewardCardName = null;`:

```js
    this.awaitingHeal = false;
```

- [ ] **Step 3: Модифицировать onRowClick — захватить def до playCard**

Заменить метод `onRowClick` целиком:

```js
  onRowClick(rowName) {
    if (this.selectedIndex === null) return;
    const playedDef = this.match.players[0].hand[this.selectedIndex].def;
    playCard(this.match, this.selectedIndex, rowName);
    this.selectedIndex = null;
    if (playedDef.effect === 'heal' && this.findWeakenedCards().length > 0) {
      this.awaitingHeal = true;
      this.render();
      return;
    }
    this.afterPlayerAction();
  }
```

- [ ] **Step 4: Добавить findWeakenedCards и onHealTarget**

Добавить два новых метода после `onRowClick`:

```js
  findWeakenedCards() {
    const board = this.match.players[0].board;
    const result = [];
    for (const row of ROW_NAMES) {
      board[row].forEach((card, i) => {
        if (card.def.type !== 'hero' && card.power < card.def.power) {
          result.push({ row, cardIndex: i });
        }
      });
    }
    return result;
  }

  onHealTarget(row, cardIndex) {
    healUnit(this.match, 0, row, cardIndex);
    this.awaitingHeal = false;
    this.afterPlayerAction();
  }
```

- [ ] **Step 5: Модифицировать renderRow — зелёная подсветка heal-целей**

В методе `renderRow`, заменить блок `side.board[rowName].forEach`:

```js
    side.board[rowName].forEach((card, i) => {
      const isHealTarget = this.awaitingHeal
        && sideName === 'player'
        && card.def.type !== 'hero'
        && card.power < card.def.power;
      const cv = createCardView(this, card.def);
      cv.setScale(0.6);
      cv.setPosition(220 + i * (CARD_W * 0.6 + 6), y);
      if (isHealTarget) {
        cv.list[0].setStrokeStyle(3, 0x00ff88).setInteractive({ useHandCursor: true });
        cv.list[0].on('pointerdown', () => this.onHealTarget(rowName, i));
      }
      this.root.add(cv);
    });
```

- [ ] **Step 6: Заблокировать пас во время выбора цели**

В методе `onPass`, добавить проверку `awaitingHeal`:

```js
  onPass() {
    if (this.match.current !== 0 || this.match.winner !== null || this.awaitingHeal) return;
    pass(this.match);
    this.selectedIndex = null;
    this.afterPlayerAction();
  }
```

- [ ] **Step 7: Убедиться что тесты всё ещё проходят**

```bash
npm test
```

Ожидаем: все 89 тестов PASS.

- [ ] **Step 8: Проверить в браузере**

Открыть `http://localhost:5173`, зайти в "Бой".

Сценарий проверки:
1. Сыграть любой юнит на поле
2. Сыграть карту "Знак" (blight/sign_damage) противника — нет, это AI. Вместо этого: в консоли браузера выполнить `match.players[0].board.melee[0].power = 1` чтобы ослабить карту.
3. Сыграть карту Медик в ряд melee
4. Проверить: ослабленная карта на поле подсвечивается зелёной рамкой
5. Кликнуть по ней — сила должна восстановиться до базовой
6. Ход переходит к ИИ

- [ ] **Step 9: Коммит**

```bash
git add src/scenes/BattleScene.js
git commit -m "feat(ui): add awaitingHeal mode — player picks heal target after Medic"
```
